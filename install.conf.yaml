- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.tmux.conf:
    ~/.zsh-aliases:
    ~/.zsh-junkdrawer:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
